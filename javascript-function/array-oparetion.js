//objective: write a funtion to give me the sum of all numbers in a array.
//step-1:declare a function.
//step-2:call check whether the function is called property.
//step-3:set a parameter(s).
//step-4:pas the parameter(s), cheak whether parameter is passed in a proper formet.
//step-5: do the function task (step by step)

function sumOfNumber(numbers) {
  let sum = 0;
  for (number of numbers) {
    console.log(number);
    sum = sum + number;
  }

  return sum;
}

const numbs = [54, 62, 12, 6];
const sum = sumOfNumber(numbs);
console.log("Sum of numbers is:", sum);
